# Auditoria de Projeto - 2026-02-04 (Revisao)

## Escopo
- Core (Rust), Server (bootstrap/push/store/turn), Desktop (Tauri/React), iOS (Swift), Android (Kotlin), Docs.

## Metodologia
- Varredura por TODO/FIXME/mock/placeholder.
- Leitura dos documentos de planejamento em `documentos/`.
- Revisao pontual de areas criticas (VoIP, midia, grupos, rede).

## Status desde a auditoria anterior
- **Corrigido RTP video basico**: seq/timestamp/ssrc gerados no envio de frame (`core/src/voip/webrtc.rs`).
- **Eventos de chamada (VoIP) expostos na FFI e iOS**: callback de eventos de chamada, handler iOS e wiring basico (`core/src/lib.rs`, `core/src/mepassa.udl`, `core/src/ffi/client.rs`, `ios/MePassa/MePassa/VoIP/CallEventHandler.swift`, `ios/MePassa/MePassa/MePassaApp.swift`).
- **DHT bootstrap storage implementado**: persistencia SQLite real (`server/bootstrap/src/storage.rs`).
- **UI de mute/desligar em video call iOS**: chamada a FFI no `VideoCallScreen`.

> Conclusao: **nao**, nem tudo que foi levantado foi implementado. Abaixo estao os gaps atuais.

## Gaps Atuais (com evidencia)

### P0 - Bloqueadores
1) **Midia continua placeholder no core**
   - `core/src/api/client.rs` (media_placeholder)
   - `core/src/network/message_handler.rs` (placeholders para media)
   Impacto: envio/recebimento de midia nao vai para a rede (multi-device quebrado).

2) **Pipeline de video incompleto**
   - `core/src/voip/video_pipeline.rs` (encode/decode TODO)
   - `core/src/voip/rtp_video.rs` (packetization VP9 TODO)
   - `ios/MePassa/MePassa/VoIP/CameraManager.swift` (TODO enviar frame via FFI)
   Impacto: video calls ainda sao experimentais e instaveis.

3) **Assinatura/seguranca de grupos com placeholder**
   - `core/src/group/manager.rs` (TODO verify signature)
   - `core/src/group/sender_keys.rs` (placeholder signing key)
   Impacto: integridade/autenticidade em grupos nao garantida.

4) **Relay/DCUtR incompletos**
   - `core/src/network/swarm.rs` (TODOs de relay/DCUtR)
   Impacto: NAT traversal e resiliencia podem falhar em cenarios reais.

### P1 - Alta prioridade
5) **VoIP iOS ainda parcial**
   - `ios/MePassa/MePassa/VoIP/CallManager.swift` (TODOs de integracao WebRTC/AVAudioEngine)
   Impacto: UI existe, mas pipeline real de audio ainda nao esta ligado.

6) **E2E/Signal e sync planejados nao implementados**
   - `core/Cargo.toml` (TODO Signal/CRDT)
   - `core/src/sync/mod.rs` (modulos comentados)
   Impacto: sync multi-device e hardening criptografico ainda fora do plano real.

7) **Secure storage por plataforma nao conectado**
   - `core/src/identity/storage.rs` (FileIdentityStorage ainda usado)
   Impacto: chaves ficam em arquivo; falta Keychain/Keystore.

8) **Push em producao e navegacao por push incompletos**
   - `ios/MePassa/MePassa/Core/PushNotificationManager.swift` (TODO peer_id real + navigation)
   Impacto: fluxos de push ainda parciais para chamadas/mensagens.

9) **Testes E2E ignorados**
   - `core/tests/message_integration.rs` (ignored + TODO)
   Impacto: baixa confiabilidade de regressao.

### P2 - Produto/UX
10) **UIs de grupos/midia continuam mock/placeholder**
    - Desktop: `desktop/src/views/GroupChatView.tsx`, `GroupListView.tsx`
    - iOS: `ios/MePassa/MePassa/Views/Group*`, `Media*`, `Profile*`, `Settings*`
    - Android: `android/app/src/main/kotlin/com/mepassa/ui/screens/group/*`, `media/*`, `settings/*`

11) **Reacoes no iOS/Android ainda stubs**
    - `ios/MePassa/MePassa/Core/MePassaCore.swift` (stub)
    - Android UI e FFI nao expostos para reactions

12) **Config placeholder**
    - `core/src/utils/config.rs`

### P3 - Docs/consistencia
13) **Documentacao desatualizada**
    - `ios/README.md` ainda fala em mock VoIP
    - Docs de fases/etapas divergem em status de features

## Validacao vs Documentos de Planejamento

### `documentos/mepassa_roadmap_atualizado.md`
- **Mensagens basicas**: core possui delivered/typing/read, mas UI wiring ainda parcial; push nao totalmente conectado.
- **Chamadas de voz (M4)**: base no core existe, mas pipeline real (audio/video + signaling completo) ainda pendente.
- **Videochamadas (M5)**: video pipeline e UIs ainda incompletas.
- **Grupos (M6)**: core parcial + UIs mock.

### `documentos/mepassa_chamadas_voip.md`
- **Signaling server WebSocket** previsto, **nao existe** como servico dedicado em `server/`.
- **Qualidade/estatisticas/eco/bt/background** nao implementados.
- **SFU para grupo** nao implementado.

### `documentos/omega_chat_project_plan.md` + `omega_core_structure.md`
- **Signal Protocol real (libsignal-client)** nao integrado; apenas base simplificada.
- **CRDT/sync multi-device** nao implementado (modulos comentados).
- **Protocol/validation/sync** ainda ausentes conforme estrutura planejada.

### `documentos/omega_docker_dev_env.md`
- Stack existe parcialmente (bootstrap/store/redis/pg/coturn), mas faltam stack dev completa e servicos de signaling/monitoring descritos.

## Plano de Implementacao Sugerido (prioridade)

### P0
1) Midia: envio/recebimento real no core + armazenamento em disco + download via peer.
2) VoIP video: completar encode/decode + packetization + envio de frames no iOS/Android.
3) Grupos: assinatura/keys reais.
4) Relay/DCUtR: integrar transporte e eventos completos.

### P1
5) Secure storage (Keychain/Keystore) e migracao de identidade.
6) Push em producao (APNs/FCM) + navigation by push.
7) Ativar testes E2E e estabilizar.
8) Signal/CRDT conforme plano (ou ajustar roadmap).

### P2
9) Fechar UIs pendentes (grupos/midia/settings/profile) em iOS/Android/Desktop.
10) Reacoes nas plataformas (FFI + UI).
11) Limpar placeholders/documentacao.

